server:
  port: 8080
  servlet:
    session:
      cookie:
        path: /

logging:
  level:
    com.hugo: debug
mybatis:
  type-aliases-package: com.hugo.pojo
  configuration:
    map-underscore-to-camel-case: true


spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db_security?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver